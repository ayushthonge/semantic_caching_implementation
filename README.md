# semantic_caching_implementation

This project implements a semantic caching system using FastEmbed for text embeddings, Qdrant as a vector database, and an LLM (gpt-4o-mini) for dynamic query responses.

-Fast semantic embeddings via FastEmbed

-Vector similarity search using Qdrant

-Automatic cache retrieval and storage

-Dynamic LLM fallback on cache miss
